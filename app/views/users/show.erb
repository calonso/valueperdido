<h1><%= t :user_profile %></h1>

<%= @user.surname %>, <%= @user.name %> |

<%= link_to "#{t :edit}", edit_user_path(@user) %>

<br />
<% if @user.passive? %>
    <%= t :passive_profile %>
<% else %>
    <%= t :active_profile %>
<% end %>
<br />
    <p>Money: <%= (AccountSummary.total_money * @user.percentage / 100).round(2) %> €</p>
<%= link_to "#{t :payment_list}", user_payments_path(@user) %>